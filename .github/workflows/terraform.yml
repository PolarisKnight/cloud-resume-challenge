# This workflow installs the latest version of Terraform CLI and configures the Terraform CLI configuration file
# with an API token for Terraform Cloud (app.terraform.io). On pull request events, this workflow will run
# `terraform init`, `terraform fmt`, and `terraform plan` (speculative plan via Terraform Cloud). On push events
# to the "main" branch, `terraform apply` will be executed.
#
# Documentation for `hashicorp/setup-terraform` is located here: https://github.com/hashicorp/setup-terraform
#
# To use this workflow, you will need to complete the following setup steps.
#
# 1. Create a `main.tf` file in the root of this repository with the `remote` backend and one or more resources defined.
#   Example `main.tf`:
#     # The configuration for the `remote` backend.
#     terraform {
#       backend "remote" {
#         # The name of your Terraform Cloud organization.
#         organization = "example-organization"
#
#         # The name of the Terraform Cloud workspace to store Terraform state files in.
#         workspaces {
#           name = "example-workspace"
#         }
#       }
#     }
#
#     # An example resource that does nothing.
#     resource "null_resource" "example" {
#       triggers = {
#         value = "A example resource that does nothing!"
#       }
#     }
#
#
# 2. Generate a Terraform Cloud user API token and store it as a GitHub secret (e.g. TF_API_TOKEN) on this repository.
#   Documentation:
#     - https://www.terraform.io/docs/cloud/users-teams-organizations/api-tokens.html
#     - https://help.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets
#
# 3. Reference the GitHub secret in step using the `hashicorp/setup-terraform` GitHub Action.
#   Example:
#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v1
#       with:
#         cli_config_credentials_token: $ {{ secrets.TF_API_TOKEN }}w

name: 'Terraform'

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      action:
        description: "Choose action to execute"
        type: choice
        options:
          - apply
          - destroy

permissions:
  contents: read # allows reading of repo contents
  pull-requests: write # can modify pull requests
  id-token: write # allows Vault OIDC if wanted

  # Ensures only one apply/destroy job can run at a given time
concurrency:
  group: terraform-state-lock
  cancel-in-progress: false

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    environment: production

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash
        working-directory: ./infrastructure

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v4


      # Connect to Tailscale to gain access to Vault
    - name: Connect to Tailnet
      uses: tailscale/github-action@v3
      with: 
        oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }} # uses the GH secrets 
        oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
        tags: tag:ci

    # Install Terraform CLI
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3


    # Fetch AWS secrets from Vault
    - name: Import secrets from Vault over Tailnet
      uses: hashicorp/vault-action@v3
      with:
        # vault node/IP
        url: https://vault.waifuprotect.org
        method: "jwt" # JSON web token is an industry-standard signed digital ID card for GH Actions
        role: "github-actions" # bound claim in role secures it even more
        secrets: | # Maps vault value to value inside the workflow
          secret/data/terraform/aws AWS_ACCESS_KEY_ID | AWS_ACCESS_KEY_ID ;  
          secret/data/terraform/aws AWS_SECRET_ACCESS_KEY | AWS_SECRET_ACCESS_KEY
    # Initialize a new or existing hosting Terraform working directory by creating initial files, loading any remote state, downloading modules, etc.
    - name: Terraform Init
      run: terraform init

    # Checks that all Terraform configuration files adhere to a canonical format
    - name: Terraform Format
      run: terraform fmt && terraform fmt -check

    # Generates an execution plan for Terraform
    - name: Terraform Plan
      if: github.event_name == 'push' || github.event_name == 'pull_request' || inputs.action == 'apply'
      run: terraform plan -input=false

      # On push to "main", build or change infrastructure according to Terraform configuration files
      # # Only run "Terraform Apply" when it is pushed to main (this includes merging)
      # Note: It is recommended to set up a required "strict" status check in your repository for "Terraform Cloud". See the documentation on "strict" required status checks for more information: https://help.github.com/en/github/administering-a-repository/types-of-required-status-checks
    - name: Terraform Apply
      if: github.ref == 'refs/heads/main' && github.event_name == 'push' || inputs.action == 'apply'
      run: terraform apply -auto-approve -input=false

    # Destroy Job
    - name: Terraform Destroy Plan
      if: github.event_name == 'workflow_dispatch' && inputs.action == 'destroy'
      run: terraform plan -destroy


    - name: Terraform Destroy
      if: github.event_name == 'workflow_dispatch' && inputs.action == 'destroy'
      run: terraform destroy -auto-approve
